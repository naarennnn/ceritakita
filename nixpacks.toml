[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo_mysql", "php82Extensions.mbstring"]

[phases.build]
cmds = ["php -m | grep mysqli"]

[start]
cmd = "php -S 0.0.0.0:$PORT"